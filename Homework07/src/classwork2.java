import java.util.*;

public class classwork2 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Введите число строк и столбцов :");
        int n = sc.nextInt();
        int m = sc.nextInt();
        int[][] matrix = new int[n][m];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                matrix[i][j] = sc.nextInt();
            }

            for (int j = 0; matrix[0].length > j; j++) {
                int columnSum = 0;
                columnSum += matrix[j][i];
            }
            for (int j = 0; j <; j++) {
                int rowSum = 0;
                rowSum += matrix[i][j];
            }




        }



            for (int k = 0; k < maxrowSum; k++) {
                int maxrowSum = 0;

            }
            for (int i = 0; i < columnSum; i++) {

            }
        }

    }

    int max = age[0];
                        for(
    int i = 1;
    i<age.length;i++)max =Math.max(max,age[i]);

